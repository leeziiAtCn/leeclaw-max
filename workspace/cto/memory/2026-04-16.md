# 2026-04-16 工作日志

## xhs-scraper skill 优化

### 问题根源（昨日教训）
`xhs-feishu-importer.js` v4 仍在脚本内调 API（Bot 身份）创建文档，导致 `feishu_doc_media` 插图时 forBidden。

### 优化内容

**1. 重写 xhs-feishu-importer.js（v5）**

彻底分离职责：
- 脚本：只做 JSONL 管理 + bitable 写入
- Agent：调用 feishu_create_doc + feishu_doc_media 创建文档和插图

新三阶段流程：
```
--prepare  → 生成 import-manifest.jsonl（含每篇 markdown + 图片路径）
Agent 读 manifest → feishu_create_doc + feishu_doc_media
--complete → 读 manifest-done → 写 bitable → 更新 JSONL
```

manifest 路径：`~/.openclaw/media/xhs-import-manifests/xhs-{phone}-manifest.jsonl`
完成文件：`~/.openclaw/media/xhs-import-manifests/xhs-{phone}-manifest-done.jsonl`

**断点续传**：Agent 每创建 20 篇写一次 manifest-done，防止中断丢失进度。

**2. 更新 SKILL.md**

- 核心脚本表：标注 v5 变化
- 工作流：2步 → 4步
- Step 1/2/3/4 各自独立说明
- "插入图片"小节简化为引用 Step 3
- 已知限制表：删除已修复项（Bot 身份），新增 300011/300031 错误

### 验证
```bash
node xhs-feishu-importer.js 16621000470 --dry-run  # ✅ 0 待导入（274 已完成）
node xhs-feishu-importer.js 16621000470 --prepare  # ✅ 正常退出
```
